# AgentOS 项目协作与方案同步

遵循上级工作区 AGENTS.md。本文件补充 AgentOS 专属要求。

## AI 开发规范与 AgentOS 的边界

- 团队工程规范的入口在 `config/harness.json`，可替换为部署团队自己的权威规范来源；实际随程序执行的共同与岗位约束在 `config/roles/`。
- 本项目采用 STO：SDD + TDD + ODD，ODD 为可观测性驱动开发。提示词约束不等于系统权限或沙箱硬门控。
- 修改规范包、角色、schema 或执行门控时必须配套回归，并同步该部署自己的实施文档和变更记录。

## 飞书方案必须与实现同步

用户于 2026-09-05 明确要求：飞书 AgentOS 方案是一份能让他人照着部署、配置、使用和维护自己团队的落地手册，不是抽象架构介绍或分阶段愿景。

每次修改 AgentOS 的架构、角色职责、功能、配置、交互、实现方式、性能、权限、部署或运维方式时，交付前必须检查是否影响方案。已采用且验证有效的更新，应同步原飞书方案的对应章节；纯内部调整若不影响使用与实现说明，可说明无需同步的理由。

1. 先读实际代码、配置示例、测试和部署验证记录，再写结论。不要把设计目标、提示词约束或仅单元测试通过说成生产保障。
2. 每个功能交代：用途、入口、配置文件与字段、操作步骤、执行链路、角色/权限边界、成功验证方式、失败排查方式。
3. 配置提供脱敏可复用示例，说明占位符如何获取、修改后是否需要重启。不得上传真实密钥、登录令牌、运行态数据库或未脱敏日志。
4. 区分角色 Markdown 的行为要求与程序/沙箱强制限制；区分聊天、任务执行、卡片按钮、人工审批。
5. Windows 与 macOS 分别描述实际启动与迁移方法；未经目标平台验证的分支必须标注。不可声称每个机器人都已配置，仅凭一个机器人测试通过不能推断全团队通过。
6. 维护者若有团队飞书实施文档，更新前先读取最新内容，保留有价值内容、资源与用户改动，按章节更新；不要持续追加互相矛盾的补丁说明。
7. 更新后回读核验，并在部署团队自己的私有同步记录中保存文档 URL/token、版本、修改章节和结果。公开仓库不提交文档 token、同步快照或用户运行数据。
8. 这是一项每次变更的交付检查要求，不等于已存在后台自动文档同步服务。不要擅自增加定时任务、共享权限或外部发布。
9. 用户于 2026-09-06 要求通用化：正文与配置示例使用“你的 AgentOS 程序路径”“你的项目路径”和项目/群 ID 占位符。真实项目仅可作为明确标注的附带示例；机器路径、NVM 布局和依赖补丁版本不得冒充通用门槛。保留实际源码兼容条件与验证方法。
10. 飞书手册必须有语义版本、更新日期及变更记录，和内部 revision、程序/依赖版本分别记录。后续更新维护首页及变更表；架构/配置不兼容升主版本，兼容能力或流程升次版本，勘误升修订号。多群说明必须区分路由/会话、共享角色与管理员、顺序 Runner、权限隔离和真实多群验收，不承诺绝对无冲突。

## 接续

阅读 README.md、`docs/getting-started.md`、`docs/architecture.md`、`docs/storage-and-data.md` 以及相关 spec/verification 文档；保留用户未提交修改。当前真实部署事实以代码和带证据的验证记录为准。

# 持续并发执行验证

版本：v1.4.0
日期：2026-09-22

## 验证对象

本次验证覆盖新建只读分析的三项变化：一张问题卡只保留一个持久 Job；源码、数据库和网页工具结果回到同一个 Codex thread；单机默认以 3 个 Runner 槽执行不同卡片。

实现边界保持不变：修改代码、数据、配置、部署或业务状态仍走原交付链与权限门。旧 `analysis_review` 任务保留旧结构，不迁移、不重跑。

## 自动化结果

- `npm test`：308/308 通过。
- `npm run check`：语法检查及 308/308 完整回归通过。
- 新回归验证源码 → 环境工具 → 源码综合只使用一个 Job ID，完成后没有 `nextJobId`。
- 新回归同时启动 3 个 Runner 槽并观察到峰值并发 3；配置低于 1 或高于 8 会拒绝启动。
- 新回归验证环境证据必须匹配当前租约、environmentId、queryId、scopeHash、读取时间、行数和结果哈希，随后写入同一 Job 上下文。
- 既有回归继续覆盖登录等待/自动恢复、共享 Chrome 的每 Job 标签隔离与授权复核、取消租约隔离、旧工作流兼容、卡片终态和完整消息投递。

第一次在受限沙箱内运行完整测试时，27 个需要监听临时 `127.0.0.1` 端口的用例收到 `EPERM`。在允许回环监听的发布检查环境中重跑后 308 项全部通过；这不是产品逻辑失败。

## 部署验收

部署后核对：

1. `/health` 返回 `analysisWorkflow=continuous-developer-tools-v2`、`runnerConcurrency=3`。
2. 本机管理控制台显示 3 个 Runner 槽的在线/忙碌数量。
3. 升级前后的 `agentos.json` 记录数量与 SHA-256 已保存，local 配置、Keychain 和 Chrome profile 未覆盖。
4. 新发真实只读问题后才验证飞书端的单 Job、并发和业务网站效果；自动化回归不等于真实 UAT/PRD 业务结论已经核实。

# 环境查询验证记录

日期：2026-09-15。平台：macOS，Node 22。新增环境访问为可选能力；缺少本机环境配置时关闭。

- 完整公共回归：140/140，通过新增 14 项环境权限与连接器回归；测试使用隔离状态、虚构身份、模拟连接器及回环 HTTP。
- 核验：PRD 成员不可领取、指定负责人批准、其他管理员拒绝、同问题关联、参数/配置/期限绑定、过期 Runner lease、重复按钮与单次领取、取消、源码阶段原子申请环境审批、无效模板受阻、只读账号直接授权、参数绑定与输出过滤、查询结果排除共享记忆。
- 当前 Mac Keychain 适配器用临时虚构凭据完成写入/读取后清理，未使用业务凭据。
- 本机真实 Codex 用虚构订单结果验证新 conversation schema、ephemeral 解释会话、仅回复而不发起任务；未发送飞书烟测消息、未连接实际环境。
- 未验证：真实 UAT/PRD 网络与数据库授权、真实 Nacos 版本兼容、业务模板准确性、群内审批按钮与实际结果显示。须完成本机凭据录入、审核模板和用户真实验收后再宣称可用。
- 当前只实现 macOS Keychain 与 Nacos v1/MySQL 连接器；Windows/Linux 凭据适配及其他数据库未实现。只读角色授权保守拒绝，不提供任意 SQL 或数据库写入。

实现和部署步骤见 [environment-access.md](environment-access.md)。生产部署的私有备份、实际进程数与文档同步记录保存在部署者本机，不提交公共仓库。

## 2026-09-16 部分结果协议与错误可见性修复

新增 environment-failure.test.js 验证环境部分结果可保存、跨范围/无领取/坏哈希/坏时间/负数证据被拒绝、重复完成被拒绝，以及错误卡片不泄漏模拟凭据。原代码分析证据校验回归继续保留。完整回归 171/171；真实 PRD 失败任务不自动重跑，远端排查缺口需新请求核验。架构和权限流程不变，本次更新文字排错说明，沿用流程图。

## 2026-09-16 数据库连接接续

完整回归175/175。新增覆盖结构化连接字段白名单、24小时有效期、跨群/项目/profile/问题/证据隔离、唯一地址自动预填、多库选择、发现完成单次接入申请、缺地址自动创建发现任务且成员PRD仍等待审批。原本机连接验证与只读授权回归继续通过。未使用实际数据库账号做业务连接验收；首次录入仍需要用户在本机完成。

## 2026-09-16 显式业务账号受控模式

178/178回归通过。新增测试：默认仍拒绝写权限、显式管理员确认、错误策略及自定义SQL拒绝、业务账号查询前必须开启只读事务、失败时不执行查询且清理连接。使用模拟数据库，不代表真实PRD账号已通过连接测试。已有配置未自动改为业务账号模式；需管理员本机确认和录入后完成真实连接验收。

## 2026-09-16 Nacos本机凭据接续

181/181回归通过。新增：同文件私有凭据解析、外部引用和多账号拒绝、来源/目标/命名空间/批准人/有效期校验、连接失败不保存、成功后Keychain适配器调用及配置/状态不含凭据。自动测试使用虚构凭据和连接适配器，不能替代真实PRD验收。新流程图与管理员确认边界同步。真实连接由群里确认卡触发，本次维护不代用户发送确认消息。

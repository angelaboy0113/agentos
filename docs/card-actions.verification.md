# 卡片按钮与交互验收 — 2026-09-05

## 已实现并验证

- 状态驱动的停止/取消、刷新、真人放行、补充信息表单；原卡更新，1000 字输入上限。
- 回调入口只接受本机认证转发；核验来源机器人、群、message_id、已发布按钮、执行版本、真人权限。
- 幂等批准和补充；旧卡拒绝控制新尝试；排队取消不再领取；旧租约和迟到完成不能复活取消任务。
- 每项任务在独立进程树执行，含工作区准备、Codex、验证命令。1500ms 控制轮询；收到取消后等待实际退出，保留文件，不影响其他 Codex。
- 停止无法确认时状态保留“正在停止”并提示人工检查，Runner 暂停领新任务；过期租约不自动重复执行，同项目停止未确认时不领新任务。
- `npm run check`：45/45 通过。新增 6 项用例含本机 HTTP 回调 → Runner 控制轮询 → 真实 Windows 测试父子进程退出 → cancelled。仅终止测试创建的进程；并非伪造群聊验收。
- 首次实际进程测试受沙箱终止权限影响；在受控本机测试环境验证通过。测试清理钩子的顺序问题已修正。未改模型或 Codex 订阅认证。
- 各状态卡片 CLI dry-run 通过；它只能验证请求构造，不代替飞书服务端 schema 校验。首次更新待补充卡片失败，后确认输入框 max_length 上限为 1000，已修正。

## 尚需外部验证

- 用户已在飞书后台为“项目负责人”添加 `card.action.trigger`；该 profile 的消费者已出现 ready 标记。其他机器人仍需分别配置，不能据此宣称全部开通。
- 用户明确回复“允许”更新现有卡片后，已移除对应暂停条目；截图卡片 om_x100b668bdc8370acc34859c93cae376 原地更新成功，revision=deliveredRevision=22，failures=0。保留原任务结论，没有创建或启动任务。
- 用户已点击“刷新状态”：2026-09-05 21:02:48（北京时间），项目负责人 refresh 回调成功，revision=deliveredRevision=23，failures=0；任务仍为 awaiting_clarification。真实刷新点击端到端通过，不代表所有按钮均已实测。
- 补充表单、取消和审批的真实群聊交互仍需验证；审批或停止真实业务任务需要选择合适任务，不用既有任务充当随意测试数据。
- macOS/Linux 的进程组终止分支未在该 Windows 主机上实机测试，迁移时需要回归。

## 独立问题（未扩大本次范围）

截图任务 JOB-20260905111232-f0631f 要求补充的原因是实际工作树只有根规范仓，未带入 21 个业务子仓。并非目标描述不清晰，也不是卡片故障；本次没有修改工作区挂载策略。
